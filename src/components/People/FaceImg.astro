---
import QuotedImage from "@components/QuotedImage.astro"
import path from "node:path";
interface Props {
    img?: string;
}

const {img} = Astro.props;
---
{img ? <QuotedImage src={path.join("/img/face", img)} width="128px" additionalClass={["max-w-none"]} /> : <></>}
